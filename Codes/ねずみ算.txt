// ステージ
//   - 「ネズミの数」
stage n as long;

// パラメーター
//   - 「一匹のネズミが産む子どもの数」
//     値を初期化していないので、
//     シミュレーション開始時に入力するよう促される
param c as int;

// 初期化
init {
    n<0> = 2;
}

// 各時刻で実行するコード
operation {
    n<next> = n<now> * c;
}
